executor.type = "uv"

[tasks]
_install = { shell = "uv sync --dev --frozen" }
_test = { shell = "pytest tests" }
_fix = { shell = 'ruff check . --fix' }
_lint = { shell = "mypy ./src" }
_format = { shell = "ruff format ." }
_init_git = { shell = "git init" }
_pre_commit = { shell = "pre-commit run --all-files" }


[tasks.clean]
help = "Remove all files generated by builds and tests."
shell = "make clean"


[tasks.run]
help = "Run the project."
shell = "uv run python -m {{cookiecutter.project_slug}}.main"


[tasks.check]
help = "Auto-fix and format the code using ruff."
sequence = ["_install", "_fix", "_lint", "_format", "_pre_commit"]
ignore_fail = false


[tasks.test]
help = "Run tests quickly using the default Python environment."
sequence = ["_test"]


[tasks.test-all]
help = "Run test and auto-fix the code."
sequence = ["check", "_test"]


[tasks.update-db]
help = "Auto update local code base on the database."
shell = "make update-db"
